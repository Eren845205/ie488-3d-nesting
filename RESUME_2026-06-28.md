# RESUME / HANDOFF — 2026-06-28 — yeni session buradan başla

> **EN GÜNCEL handoff.** Bu oturum: **APP/UX otomasyon işi** (nesting motoru DEĞİL) —
> otonom akışı asenkron+canlı ilerleme yapıldı, kalıcı İş Geçmişi + detay sayfası eklendi,
> hocanın istediği **OTOMATİK MOD** (sistem kendi tarar) UI'ya bağlandı. Önceki: `RESUME_2026-06-27.md`
> (akıllı mod seçimi K-16, motor tarafı). Branch `m1-cavity-nfv`, git **TEMİZ**. Bu oturum **3 commit**:
> `2545cf0` (asenkron+geçmiş), `de167f0` (geçmiş detay), `faad85d` (otomatik mod).

---

## 0. İLK İŞ (yeni session)
0. **📊 KARAR VERİTABANI** `MOTOR/YONTEM_HARITASI_DATA_BASE.md` — **nesting MOTORU** işi (kalite/hız) için.
   Bu oturum motor değil APP tarafı; motor durumu 06-27'den DEĞİŞMEDİ (auto/K-16 aynı).
1. Bu dosyayı oku. App tarafı: `APP_YOL_HARITASI.md` + bu oturum özeti aşağıda.

---

## 1. BU OTURUMDA YAPILAN — APP otomasyon/UX (3 commit)

### Başlangıç: uçtan uca CANLI test (gerçek Gmail + Ollama) — kritik bulgu
Otonom butonu gerçek uygulamada test edildi: **çalışıyor** (gerçek Gmail'den ZIP-STL siparişleri
çekildi, auto mod işledi, HTTP 200) **AMA ~9 dk sürdü** → tarayıcı timeout + sunucu blok.
- Auto mod gerçek veride **NFV** seçti (`mean_aspect_z=11.5`, `thin_plate=0.17` → cavity-aday, kural doğru).
- GPU yok (`CUDA_PATH` algılanmadı) → NFV **CPU'da 564s**. Sonuç tablosu "Baseline'a Kazanç **%0**" →
  bu sette NFV pratik kazanç sağlamadı ama 9 dk harcadı. **→ AÇIK İŞ: auto mod GPU-farkındalığı (§2).**

### Commit `2545cf0` — Asenkron otonom + kalıcı İş Geçmişi
- `src/runtime/otonom_jobs.py` **OtonomJobStore** (kilitli, tek-iş, thread-güvenli).
- `app.py::_run_otonom_pipeline` refactor: senkron `/otonom` + async ORTAK gövde (request-bağımsız,
  `on_stage` ile aşama-aşama ilerleme). **Senkron `/otonom` BİREBİR korundu** (32 mevcut test yeşil).
- `/otonom/baslat` (202+job_id) + `/otonom/durum/<id>` (snapshot) + `app.run(threaded=True)`.
- Frontend: "başlat + 2sn yokla"; **forge canlı panel** (`forge-status` durum şeridi + amber nabız +
  geçen-süre sayacı) — "çöktü mü çalışıyor mu" belirsizliğini çözer. CSS: style.css forge-pipeline bloğu.
- `src/runtime/otonom_gecmis.py` **OtonomGecmisStore** (kalıcı JSONL, `data/otonom_gecmis/`).

### Commit `de167f0` — İş Geçmişi detay sayfası (AYRI sayfa)
- Geçmiş satırına tıkla → `/gecmis/<id>` AYRI sayfa (liste işgal olmaz) → "← Geri" ile döner.
- `gecmis_detay.html`: nesting modu kararı (auto→seçilen mod + şeffaf gerekçe), 6 metrik kartı,
  işlem hattı aşama timeline'ı, 3D notu. Geçmiş kaydı zenginleşti: asama özeti + secilen_mod + doluluk.

### Commit `faad85d` — OTOMATİK MOD (hocanın isteği) + geçmiş entegrasyonu
- **Motor zaten vardı:** `src/runtime/mail_poller.py` (MailPoller daemon thread, periyodik tarar,
  `/poll/baslat|durdur|durum`, `MAIL_POLL_ENABLED` env). UI'ya bağlı değildi → bağlandı.
- `_gecmis_kaydet` ortak helper (DRY): manuel /otonom (`kaynak=manuel`) + otomatik poller
  (`kaynak=otomatik`) AYNI yoldan geçmişe yazar. `_poll_on_result` artık geçmişe de yazar.
- `/poll/baslat` opsiyonel `interval_s` (2/5/10 dk; min 30s).
- UI: **Otomatik Mod kartı** (ana sayfa) — forge amber **toggle** + canlı durum şeridi
  ("Gözcü aktif · her N dk · son tarama · X iş işlendi · Y tarama") + aralık seçimi. Manuel buton
  AYNEN kalır; ikisi yan yana. `/gecmis` + detay: kaynak rozeti (⚙ otomatik / ☞ manuel).
- **Otomasyon sınırı (kullanıcı kararı):** nesting+fiyat otomatik, ama **teklif maili otomatik
  GÖNDERİLMEZ** — insan onay kapısı korunur.
- **Idempotency (aynı mail iki kez işlenmez) ZATEN kalıcı:** `ImapMailbox._seen_uids` +
  `SqliteIdempotencyStore` (`imap:{user}:{uid}`). Poller yalnız YENİ mailleri işler; restart-güvenli.

**Test:** Tam suite **2126 passed** (06-27 sonrası +48 yeni: jobs 15, async 8, gecmis depo 9,
gecmis sayfa/detay 9, otomatik 7). Regresyon yok. Canlı ilerleme + geçmiş + otomatik mod
gerçek tarayıcıda (Playwright) görsel doğrulandı.

---

## 2. AÇIK / SONRAKİ İŞLER
### APP tarafı (bu oturumun devamı)
- **Otomatik mod GERÇEK uçtan uca demo:** otomatik aç → gerçek test maili at → sistem kendiliğinden
  yakalayıp işlesin → `/gecmis`'te ⚙ otomatik kaydı gör. (Henüz yalnız FakeMailbox + UI doğrulandı;
  gerçek Gmail poll-yakalama canlı izlenmedi.)
- **⚠️ Auto mod GPU-FARKINDALIĞI (kritik UX bulgusu):** GPU'suz makinede auto, cavity-aday ama
  pratikte %0-kazanç veren sette NFV seçip 9 dk harcıyor. Çözüm fikri: GPU yok + büyük-N + düşük
  cavity-sinyali → heightmap'e düş (kalite-güvenliği koru, K-16 felsefesi). **ÖLÇ-ÖNCE:** önce bu
  sette heightmap gerçek süre+kalite ölç. (Motor/auto tarafı; YONTEM_HARITASI §5 C1 ile örtüşür.)
- Geçmiş detayında **3D önizleme** (şu an v2-notlu; her iş için geometri saklamak gerekir = büyük).

### Nesting MOTORU tarafı (önceki açık, değişmedi)
- Görev #5 süre-derinleştirme (`coarse_to_fine` FINE 159s/parça @0.5mm, pitch R6) — riskli,
  ÖLÇ-ÖNCE. Detay `RESUME_2026-06-27 §2` + YONTEM_HARITASI §5 C1.
- Magics %6 kalite açığı = süper bilgisayar (A1). YONTEM_HARITASI §5.

---

## 3. OPERASYONEL NOTLAR
- **Asenkron mimari:** `/otonom` (senkron, geriye-uyum) + `/otonom/baslat`+`/otonom/durum` (async job).
  `_run_otonom_pipeline` ortak gövde; senkron testler koruma ağı. `threaded=True` şart (polling).
- **İş geçmişi:** `OtonomGecmisStore` JSONL `data/otonom_gecmis/` (gitignore'da). `_gecmis_kaydet`
  helper hem manuel hem poller. Kayıt: kaynak/mod/secilen_mod/reason/müşteri/parti/yükseklik/doluluk/
  fiyat/süre/asamalar.
- **Poller:** `MailPoller` daemon thread; `PollState.snapshot()` UI'ya. `/poll/durum` GET (CSRF muaf
  değil ama okuma). Test FakeMailbox idempotency'yi taklit eder (ilk fetch dolu, sonraki boş).
- **Login:** ADMIN_PASSWORD set (`.env`, `Sifre123`) → /gecmis dahil tüm sayfalar login+CSRF ister
  (testing=False). Playwright testinde login akışı gerekti.
- cp1254 stdout: emoji/sembol print çökertir → ASCII-safe ([[feedback-windows-stdout-ascii]]).

## 4. DOSYALAR (bu oturum)
**Yeni:** `src/runtime/{otonom_jobs,otonom_gecmis}.py`, `src/webapp/templates/{gecmis,gecmis_detay}.html`,
`tests/{test_otonom_jobs,test_otonom_gecmis,test_webapp_gecmis,test_webapp_otonom_async,test_webapp_otomatik}.py`.
**Değişen:** `src/webapp/app.py` (asenkron+poller+geçmiş+helper), `src/webapp/static/style.css`
(forge-pipeline+switch+gecmis-row), `src/webapp/templates/{base,index}.html`, `.gitignore`.
**Önceki handoff:** `RESUME_2026-06-27.md` (K-16 motor); karar veritabanı `MOTOR/YONTEM_HARITASI_DATA_BASE.md`.
