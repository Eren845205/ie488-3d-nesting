[pytest]
# Marker definitions — PLAN_DEMO1.md Faz 0.3
# Default run (`pytest`) skips slow-marked tests.
# Full run: `pytest -m slow` or `pytest -m "slow or not slow"` (all).
markers =
    slow: long-running tests (voxelization of real STL parts — several minutes)

addopts = -m "not slow"
