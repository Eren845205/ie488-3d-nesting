# RESUME / HANDOFF — 2026-06-28b — yeni session buradan basla

> **EN GUNCEL handoff.** Bu oturum: **otomatik mod (mail poller) SAGLAMLASTIRMA** —
> gercek Gmail ile CANLI uctan-uca demo yapildi, demo 3 kok sorunu ortaya cikardi,
> ucu de TDD + reviewer + canli-dogrulama ile kapatildi. Onceki: `RESUME_2026-06-28.md`
> (APP otomasyon: asenkron + gecmis + otomatik mod UI). Branch `m1-cavity-nfv`, git **TEMIZ**.
> Bu oturum **3 commit**: `f5718c7` (P0) + `4b08b86` (P1) + `22622db` (P2).

---

## 0. ILK IS (yeni session)
0. **KARAR VERITABANI** `MOTOR/YONTEM_HARITASI_DATA_BASE.md` — nesting MOTORU isi icin.
   Bu oturum motor DEGIL, APP/poller tarafi; motor durumu 06-27'den degismedi (auto/K-16 ayni).
1. Bu dosyayi oku. App tarafi: `APP_YOL_HARITASI.md` + bu oturum ozeti asagida.

---

## 1. BU OTURUMDA YAPILAN — otomatik mod saglamlastirma (3 commit)

### Baslangic: gercek Gmail CANLI uctan-uca demo (otomatik mod)
Onceki oturum otomatik modu (poller UI) yalniz Fake+UI ile dogrulamisti. Bu oturum
GERCEK Gmail'e baglandi. Demo, kodun forward ZIP'ini dogru isledigini kanitladi
(sahte-pipeline: 2 forward -> 2 ZIP-STL siparisi -> run_pipeline'a ulasti) AMA poller
turunda forward siparisi pipeline'a hic girmedi. Teshis **3 kok sorun** buldu:

### Commit `f5718c7` — P0: idempotency turlar-arasi KALICI store
- **Bug:** poller HER tarama turunda taze ImapMailbox uretiyordu; her instance kendi
  `:memory:` SqliteIdempotencyStore + bos `_seen_uids` aliyordu -> idempotency state
  turlar arasi PAYLASILMIYORDU. Gercek Gmail'de son 20 mail HER turda yeniden islenir.
  (RESUME_2026-06-28'in "idempotency ZATEN kalici/restart-guvenli" iddiasi yalniz TEK
  ImapMailbox omru icin gecerliydi; cok-turlu poller icin DEGILDI.)
- **Fix:** `ImapMailbox.__init__` + `make_mail_source`'a opsiyonel `idem_store` enjeksiyonu
  (None -> mevcut `:memory:`, geriye uyumlu). app.py poller icin TEK paylasimli kalici
  `SqliteIdempotencyStore(data/idempotency.db)`. `_idem_key` SHA-256 hash (email plaintext
  yazilmaz). `_conn` her thread-conn'da schema kurar (no-such-table fix).
- **Canli kanit:** cok-tur Tur1=20 mail -> Tur2=0; disk restart taklidi -> 0.

### Commit `4b08b86` — P1: at-least-once (isle-sonra-register, SIPARIS KAYBI onle)
- **Bug (P0 fix'in ortaya cikardigi):** `fetch_new` maili CEKERKEN register ediyordu
  (pipeline'dan ONCE) -> "at-most-once". Kalici store ile: forward maili register edilir
  ama o turda islenmezse (yavaslik/restart/exception) BIR DAHA denenmez = KALICI KAYIP.
  Canli Gmail'de forward siparisi tam boyle kayboldu (idempotency.db'de kayitli ama
  pipeline hic calismadi).
- **Fix:** fetch-dedup (`_seen_uids`, bellek, bu-process) ile idempotency (`_idem_store`,
  kalici) AYRILDI. `RawMail.uid` alani; `MailSource.mark_processed(mail)` concrete no-op
  (ImapMailbox override -> kalici store). `fetch_new` artik register ETMEZ. process_inbox_once
  `mark_processed` YALNIZ kesin sonucta: spam (ingest None) + needs_review HEMEN; order-ureten
  mailler ANCAK run_pipeline BASARISINDAN sonra. Pipeline/ingest exception -> register YOK ->
  sonraki tur tekrar dener. mark_processed: DuplicateKeyError yutulur, diger store hatalari LOGLANIR.
- **Canli kanit:** UID 6577 -> RawMail.uid='6577' -> mark_processed -> store True; uid=None -> no-op.

### Commit `22622db` — P2: spam sinyal kapisi + ek-li mail onceligi
- **Bug:** poller turu spam/seyahat mailleriyle (booking/mytrip) doluyordu cunku 2D olcu
  "80x60" / "12x9" desenleri `_has_order_signal`'i YANLIS-POZITIF tetikleyip her birini
  yavas LLM'e (3 retry) sokuyordu; gercek ZIP siparisi kuyrugun arkasinda bekliyordu.
- **Fix P2.1:** `_DIM_PATTERN` 2D(NxN) -> 3D(NxNxN). Gercek parca 3 boyutlu; demo siparisleri
  (Ford 80x60x30, Baykar 90x45x20) 3D+"adet" cift-korumali. parse_quantities (TR "adet") aynen.
- **Fix P2.2:** `mail_poller._attachment_first` — process_inbox_once ingest LOOP'unda ZIP/Excel
  ekli mailleri basa alir (kararli sort). Kesin siparis spam-LLM gecikmesinden ayrilir.

### SONUC — gercek Gmail CANLI uctan-uca (P0+P1+P2 hepsi tek demoda)
Otomatik mod acildi, poller iki forward'i (Plan1.zip+Plan3.zip) kendiliginden yakaladi ->
ek-oncelik ile isledi -> ZIP yolu -> pipeline -> **`/gecmis`'e `kaynak=otomatik` kaydi dustu**:
siparis_sayisi=2, musteri=HOTMAIL, secilen_mod=nfv, doluluk=0.369, fiyat=200, **sure=596sn**.
- **P2.1 carpici:** 20 spam "sinyal yok", **0 LLM** (onceki demoda 8 LLM yuku vardi -> sifir).
- **P1 kanit:** pipeline basarili -> mark_processed -> register (idempotency.db forward dahil).
- **Tam suite: 2167 passed** (2144 taban + 16 P1 + 7 P2), 0 fail.

---

## 2. ACIK / SONRAKI ISLER
### P3 — auto mod GPU-FARKINDALIGI (bu oturum CANLI DOGRULADI, kapsam disiydi)
- Canli demo NFV suresini **596 sn (~10 dk)** olcdu: auto, cavity-aday seti (mean_aspect_z=9.9)
  NFV secti, GPU yok -> uzun. Bu, RESUME_2026-06-28 §2'deki "auto GPU-farkindaligi" acik isi.
- **Cozum yonu:** GPU yok + buyuk-N + dusuk-cavity-sinyali -> heightmap'e dus (K-16 felsefesi).
  **OLC-ONCE:** bu sette heightmap gercek sure+kalite olc. YONTEM_HARITASI §5 C1. **Motor tarafi.**
- Plan dosyasi: `~/.claude/plans/robust-swimming-crown.md` (P1+P2 detay; P3 "kapsam disi" notu).

### Diger (onceki, degismedi)
- Otomatik mod GERCEK Gmail demo ARTIK YAPILDI (yukarida) — onceki acik kalem kapandi.
- Gecmis detayinda 3D onizleme (buyuk veri); motor #5 sure-derinlestirme (YONTEM_HARITASI §5).

---

## 3. OPERASYONEL NOTLAR
- **Idempotency mimarisi (P0+P1):** fetch-dedup `_seen_uids` (bellek, bu-process, restart'ta
  sifirlanir) AYRI; idempotency `_idem_store` (kalici `data/idempotency.db`, gitignore'lu)
  YALNIZ `mark_processed` ile (kesin islem sonrasi) yazilir. Poller make_source her tur taze
  ImapMailbox ama AYNI paylasimli store enjekte eder (app.py `_shared_idem_store`).
- **at-least-once:** pipeline basarisizsa order-mailleri register EDILMEZ -> sonraki tur tekrar.
  Yan etki: ayni siparis iki kez islenip iki ⚙ kaydi uretebilir (pipeline idempotent degil) —
  reviewer LOW/dikkat notu; su an kabul (downstream dedup gelecek is).
- **Sinyal kapisi:** 3D(NxNxN) VEYA TR "adet" -> LLM'e gir; 2D tek basina DEGIL. Ekli mailler
  (ZIP/Excel) kapidan bagimsiz (deterministik yol, LLM yok) + `_attachment_first` ile once islenir.
- **Login:** ADMIN_PASSWORD=.env (Sifre123); /gecmis dahil login+CSRF. Poller /poll/durum login arkasi.
- cp1254 stdout: log/print/reason ASCII-safe ([[feedback-windows-stdout-ascii]]). Em-dash `—`
  cp1254'te kodlanabilir (cokmez) ama kural geregi yeni log'larda `--` kullan.

## 4. DOSYALAR (bu oturum)
**Yeni:** `tests/{test_mail_poller_idempotency,test_at_least_once_idempotency,test_poller_attachment_priority}.py`.
**Degisen:** `src/runtime/{mail_ingest,mail_poller,idempotency}.py`, `src/webapp/app.py`,
`tests/test_order_signal_gate.py`, `.gitignore` (data/idempotency.db).
**Onceki handoff:** `RESUME_2026-06-28.md`; karar veritabani `MOTOR/YONTEM_HARITASI_DATA_BASE.md`;
plan `~/.claude/plans/robust-swimming-crown.md`.
