# RESUME 2026-07-02 — Güvenlik + İşlevsellik Denetimi Oturumu

> Bir önceki handoff: `RESUME_2026-06-30.md` (P3 GPU 596s-seti doğrulama).
> Karar veritabanı: `MOTOR/YONTEM_HARITASI_DATA_BASE.md`. Bellek: `project-guvenlik-denetim-2026-07.md`.

## Bu oturumda ne yapıldı

**GENEL DENETİM** (kullanıcı talebi: "hem güvenlik hem uygulama çalışması açısından genel tarama + rapor").
5 paralel uzman denetçi (reviewer agent) → **1 CRITICAL, 10 HIGH, 9 MEDIUM, 9 LOW**.
Tam rapor: repo kökünde **`DENETIM_RAPORU_2026-07-01.md`**.

Ardından **3 TDD dalgası ile 12 üretim-blokeri KAPATILDI** (~47 yeni test, tam suite **2176 → 2223 passed, 0 regresyon**).

### Commit'ler (branch `m1-cavity-nfv`, HENÜZ PUSH EDİLMEDİ)
- `54ce354` docs — denetim raporu
- `a9c61a8` fix(input/dos) — **C1** MAX_QTY=5000 clamp (parser/order_attachment/quantity_text + demo_pipeline parti guard) · **H2** zip-bomb (file_size ön-eleme + chunk okuma) · **H8** jsonschema zorunlu
- `fa2b1ba` fix(automation) — **H5** excel/csv boş parts→needs_review (sonsuz-retry kapandı) · **H6** mail_poller `_inflight_lock` (restart-yarışı) · **M5** PollState tek-lock  *(Opus override — thread-yarışı riskli lane)*
- `5579a15` fix(webapp-security) — **H1** /adet-gir path-traversal (sanitize + containment guard) · **H3** /giris 5/dk rate-limit · **H4** manuel/otomatik ortak idempotency + `_scan_lock` · **M1** fail-loud LAN uyarı · **M2** cookie flags · **M4** /run 3/dk

## KALDIĞIMIZ YER — sonraki oturum seçenekleri (hiçbiri üretim-blokeri değil)

Öncelik sırasıyla önerilen kalan işler:
1. **H9 (kolay, yüksek değer):** README kökten eski — yalnız temel 2D/3D akademik projeyi anlatıyor; NFV/GPU/mail-otomasyonu/fiyat/seçim-modeli YOK. Gerçek mimariye güncelle.
2. **H7 (güvenlik derinliği):** Prompt injection tespiti hâlâ YALNIZ LLM'in `injection_suphesi` beyanına dayanıyor (tek katman). Deterministik regex ön-tarama ekle (`ignore/disregard/system prompt/yönergeyi görmezden gel` TR+EN), LLM kararıyla **OR**'la. Dosya: `src/llm/roles/parser.py`, `src/runtime/mail_ingest.py:877-887`.
3. **H10 (kanıt tazeliği):** Canlı doğrulama bu turda YENİDEN koşulmadı = NEEDS_RUNTIME_CHECK. Çalıştır: `pytest tests/ -m gpu -q` + gerçek Flask `/poll/baslat`+`/poll/durum` HTTP probe (runs 0→1 gözle).
4. **Orta (MEDIUM):** M3 (CSV /siparisler/csv O(n²) → tek okuma/yazma + satır limiti) · M6 (sabit report_path çakışma) · M7 (teklif/watcher/assistant prompt'larına "veri, talimat değil" çerçevesi) · M8 (`payload_logging=full` PII sızıntısı + retention silme job'ı yok) · M9 (`adaptive_params.recommend` production'a bağlı değil — ya bağla ya temizle).
5. **Düşük (LOW):** L1 (32-bit order_id hash → 12+ hex), L2 (ham exc istemciye), L3 (CSRF token login rotate), L4 (termin makullüğü), L7 (plate.py:34 docstring %10 yanlış→%2), L8 (yanıltıcı CSRF yorumu app.py:2541).

## 2026-07-02 DEVAM OTURUMU — C1 voxelize hızı (H-14) KAPANDI ✅

Kullanıcı "kalite/hız nerede kaldık" → tek açık 6GB kaldıracı **C1**'e girildi (ÖLÇ-ÖNCE):

1. **Profil** (`scripts/c1_voxprofile.py`, P155308 @0.5mm, 150s/oryantasyon):
   %84 `_surface_cells` (915M barycentric nokta: pts 84s + mark 41.5s), %13 `contains_xy`, %3 section.
2. **Prototip + kapı** (`scripts/c1_voxspeed.py`): eksen-bazlı `_surface_cells` (out= buffer,
   int32, aynı işlem sırası = bit-özdeş) + `_slice_voxelize` bbox-kırpma. **9/9 gerçek parça
   birebir GEÇTİ** (Plan2 + mail Plan1/Plan3; pitch 2.0/1.0/0.5). 0.5mm: surface 3.1×, slice 3.2×.
3. **Üretim** (`voxelize.py`) + donmuş-referans testleri (`tests/test_voxelize_c1_exact.py`, 8 test).
   **Tam suite 2231 passed, 0 regresyon.** Commit `ff3b02a`.

**Etki:** fine adım 159→~50s/parça (3.1×); NFV'de paylaşılan voxelize payı küçüldüğünden GPU
uçtan-uca kazancı da büyür (§5 C1 gerekçesi). Kayıt: YONTEM_HARITASI **H-14** + hız özeti + §5.

**C1'den kalan (ayrı karar):** pitch politikası R6 (tek 1mm parça → 356mm parça da 0.5mm) —
parça-kaybı + H-06 duvarı + cross-dataset riski; girilmedi.

## 2026-07-03 DEVAM — KALİTE: K-17 fine z-kompaksiyon probu GO ✅ (üretime BAĞLANMADI)

Kullanıcı "kalitede de benzer etki yapabilir misin" → kuantizasyon-vergisi hipotezi ölçüldü
(`scripts/c1_fine_zcompact.py`, commit `e6fb06a`): NFV @2.0mm layout pozisyon-koruyarak 0.5mm'de
oturtulunca **plan3 844→830.5 (+%1.6) · plan2 522→516 (+%1.1, Magics açığı %6.1→%4.9) ·
plan1 116→115.5 (+%0.4)**. FFT'siz (~500MB), H-14 sayesinde ekonomik (~2dk/sipariş).
KRİTİK DERS: alçaltma yalnız herkes yerleşikken (ilk sürüm plan3'ü -123mm bozmuştu; yerleştir-VE-otur
ayrımı). Detay: YONTEM_HARITASI **K-17** + KALİTE ÖZET güncellemesi + §5 **K-17p**.

**SIRADAKİ İŞ (K-17p):** solve_nfv'ye post-pass bağla — fine=pitch/4, bellek pre-flight,
"h iyileşmezse coarse korunur" guard'ı, TDD + tam suite. Opsiyonel: n=12 kombinasyon ölçümü.

## 2026-07-03 DEVAM-2 — K-17p ÜRETİMDE ✅ + K-18 ölçüldü

- **K-17p KAPANDI (commit `07f697b`):** `src/nesting3d/fine_settle.py` — `solve_nfv fine_settle=True`
  **default-on** (guard: iyileşmezse/bellek yetmezse/hata → coarse aynen korunur). Sonuç fine pitch
  çerçevesinde döner; `demo_pipeline pitch_mm` artık sonucun pitch'i (GLB/STL hizası). 7 yeni test;
  **tam suite 2238 passed, 0 regresyon**. Artık her NFV koşusu vergiyi otomatik tahsil ediyor
  (`adaptive_reason`'da "settle a->b mm" izi).
- **K-18 ölçüldü (plan2):** n=24 eksen-hizalı (AX24, eğiksiz) + settle = **512.5mm**
  (n=8 baz 522'den −9.5mm = %1.8; Magics açığı %6.1→**%4.2**). Sanity: n24 baz 520.0 = K-13 A24
  birebir. Bedel decode ~4× CPU. **AÇIK: K-18p** — AX24'ü `quality="max"` merdivenine bağlama
  (RAM-tavanı + cross-dataset ister; default'a değil). YONTEM_HARITASI K-18 + §5 K-18p.

## 2026-07-03 DEVAM-3 — K-18p ÜRETİMDE ✅ + cross-dataset 3/3 KAPANDI

- **K-18p (commit `f44ee80`):** `quality="max"` artık **AX24** (24 eksen-hizalı, eğik 8..11 hariç —
  K-13 kanıtı); RAM<13GB → n=8 taban. `to_voxel_parts allowed_orientations` + `_quality_max_orientations`.
  Tam suite **2240 passed**. Fine-settle otomatik biner (indeks-bağımsız).
- **Cross-dataset (n8 baz → n24+settle):** plan1 116→**108 (−%6.9)** · plan2 522→**512.5 (−%1.8**,
  Magics açığı **%4.2)** · plan3 844→**755.5 (−%10.5!)**. Ayrışım: plan2 settle-baskın, plan1
  poz-baskın, plan3 ikisi birden (−64 poz −24.5 settle) → kaldıraçlar TAMAMLAYICI.
- **Probe `prod` modu:** `c1_fine_zcompact <ds> 0.5 n24 prod` → decode üretim `best_decode`
  (gpu-resident; sanity plan1 116.0 BİREBİR, 616→45s = 13.6×). Gelecek ölçümler 10× ucuz.
- **GÜN TOPLAMI (07-02→03):** H-14 voxelize 3.1× (birebir) + K-17 settle üretimde (NFV default) +
  K-18 AX24 üretimde (quality=max). Kalan yapısal açık = A1 (süper bilgisayar).

## Notlar
- Değişiklikler branch `m1-cavity-nfv`'de, **push edilmedi** (kullanıcı isterse push).
- Tam bulgu detayı + istismar senaryoları: `DENETIM_RAPORU_2026-07-01.md`.
- Doğrulanan sağlam noktalar (tekrar dokunma): .env git-temiz, compare_digest, global CSRF, fiyat/termin deterministik + insan onay kapısı, çekirdek poller (kalıcı idempotency/at-least-once/ghost-thread-yok), overfit gate.
