# RESUME / HANDOFF — 2026-06-29 — yeni session buradan basla

> **EN GUNCEL handoff.** Bu oturum: **DOWNSTREAM DEDUP** (06-28b'de secilen oncelik)
> kapatildi. Onceki: `RESUME_2026-06-28b.md` (otomatik mod poller saglamlastirma,
> 3 commit). Branch `m1-cavity-nfv`, git **TEMIZ**. Bu oturum **1 commit**: `924cbdc`.

---

## 0. ILK IS (yeni session)
0. **KARAR VERITABANI** `MOTOR/YONTEM_HARITASI_DATA_BASE.md` — nesting MOTORU isi icin.
   Bu oturum motor DEGIL, APP/gecmis tarafi; motor durumu degismedi.
1. Bu dosyayi oku. App tarafi: `APP_YOL_HARITASI.md` + bu oturum ozeti asagida.

---

## 1. BU OTURUMDA YAPILAN — downstream dedup (commit `924cbdc`)

### Problem (06-28b'den devralinan secilen oncelik)
P1 at-least-once'in bilincli yan etkisi: pipeline yarida kalir / mark_processed
basarisiz olursa sonraki tur AYNI forward maili yeniden isler -> **iki ⚙ gecmis
kaydi + iki pipeline (CPU)**. (Teklif maili zaten insan-onayli, otomatik gitmez.)

### Cozum
Deterministik `order_id = ZIP-{sha256(message_id)[:8]}` (mail_ingest `_ingest_zip_stl_order`)
dedup anahtari olarak kullanildi -> ayni mail HEP ayni order_id -> idempotent kayit.

- **`src/runtime/otonom_gecmis.py`**: `kaydet(ozet, *, dedup_key=None)`. dedup_key
  verilince ayni `_dedup_key`'li kayit varsa YAZMA, mevcudu dondur. Kontrol-oku-yaz
  TEK `self._lock` icinde atomik (race-safe). `dedup_key=None` -> eski davranis (geriye uyum).
  Lock refactor: `_oku_kilitsiz` (lock'suz, yalniz lock icinden cagrilir) + `_oku_hepsi`
  (lock alir). `liste()/get()` artik `_strip_internal` ile `_`-onekli ic alanlari ayiklar
  (dedup kontrolu `_oku_kilitsiz` uzerinden oldugu icin BOZULMAZ).
- **`src/webapp/app.py`** `_gecmis_kaydet`: ranked'dan `order_id`'leri toplar
  (`sorted(set(...))`), **yalniz `kaynak=="otomatik"` ve order_ids doluysa**
  `dedup_key=json.dumps(order_ids)` (collision-safe; eski `"|".join` degildi).
  order_ids bos ama otomatik+ranked -> uyari loglar, kayit yine yazilir (kayip yok).
  Kayda `order_ids` alani eklenir. Test erisimi icin `app.config["GECMIS_KAYDET_FN"]`.

### Reviewer (subagent) sonucu: 0 CRITICAL / 0 HIGH
Cekirdek atomiklik dogru onaylandi (10-thread race testi gecer). Uygulanan:
- **M1** dedup_key ayirici `"|".join` -> `json.dumps` (collision-safe).
- **M2** `_dedup_key` ic alani `liste()/get()`'ten ayiklandi (tarayiciya sizmaz) + test.
DEFER (bilincli, tek-operator deployment):
- **M3** lock-ici O(N) full-file tarama her dedup yaziminda -> kabul (otonom isler seyrek;
  ileride `self._dedup_index: set` lazy-cache ile O(1) yapilabilir).
- **M4** `threading.Lock` cok-PROCESS korumaz (yalniz cok-thread). Tek-operator
  pywebview/tek-sunucu icin yeterli; cok-process planlanirsa `msvcrt.locking`/`flock`.
- **M5/L'ler**: order_ids tum kayitlara eklenir (zararsiz); birkac edge-case testi opsiyonel.

### Test
18 dedup testi (`test_otonom_gecmis_dedup.py` 6 birim: ayni/farkli/None/kalici/race/sizinti
+ `test_webapp_gecmis.py` TestGecmisDedupOtomatik 3 app-katmani). **Tam suite: 2176 passed.**

---

## 2. ACIK / SONRAKI ISLER

### >>> P3 — auto mod GPU-FARKINDALIGI (motor tarafi, su an EN ONEMLI acik) <<<
06-28b canli demo NFV suresini **596 sn (~10 dk)** olcdu: auto, cavity-aday seti
(mean_aspect_z=9.9) NFV secti, GPU yok -> uzun.
- **Cozum yonu:** GPU yok + buyuk-N + dusuk-cavity-sinyali -> heightmap'e dus (K-16 felsefesi).
  **OLC-ONCE:** bu sette heightmap gercek sure+kalite olc. YONTEM_HARITASI §5 C1. **Motor tarafi.**

### Diger (degismedi)
- Gecmis detayinda 3D onizleme (buyuk veri); motor #5 sure-derinlestirme (YONTEM_HARITASI §5).

---

## 3. OPERASYONEL NOTLAR
- **Dedup mimarisi:** dedup_key YALNIZ `kaynak=otomatik` (mail kaynakli) kayitlarda; manuel
  calistirmalar her zaman ayri kayit (dedup_key=None). Anahtar = json.dumps(sorted order_ids).
  Kalici: `_dedup_key` diske yazilir; yeni store ornegi ayni jsonl'den okuyunca dedup calisir.
- **Idempotency mimarisi (06-28b, degismedi):** fetch-dedup `_seen_uids` (bellek) AYRI;
  idempotency `_idem_store` (kalici `data/idempotency.db`) YALNIZ `mark_processed` ile.
- **Login:** ADMIN_PASSWORD=.env (Sifre123); /gecmis dahil login+CSRF.
- cp1254 stdout: log/print/reason ASCII-safe ([[feedback-windows-stdout-ascii]]); yeni log'larda `--`.

## 4. DOSYALAR (bu oturum)
**Degisen:** `src/runtime/otonom_gecmis.py`, `src/webapp/app.py`, `tests/test_webapp_gecmis.py`.
**Yeni:** `tests/test_otonom_gecmis_dedup.py`.
**Onceki handoff:** `RESUME_2026-06-28b.md`; karar veritabani `MOTOR/YONTEM_HARITASI_DATA_BASE.md`.
