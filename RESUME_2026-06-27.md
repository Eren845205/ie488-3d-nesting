# RESUME / HANDOFF — 2026-06-27 — yeni session buradan başla

> **EN GÜNCEL handoff.** Bu oturum: **akıllı otomatik nesting mod seçimi** (NFV/heightmap) üretime
> bağlandı + kalite-güvenliği KANITLANDI. Önceki: `RESUME_2026-06-26.md` (mail-fix + voxelize OOM +
> çift-voxelize). Branch `m1-cavity-nfv`, git **TEMİZ**. Bu oturum 1 commit: `c89041c`.
> Rollback: `checkpoint-2026-06-27-pre-automode` (+ önceki checkpoint'ler).

---

## 0. İLK İŞ (yeni session)
0. **📊 KARAR VERİTABANI** `MOTOR/YONTEM_HARITASI_DATA_BASE.md` (K-16 akıllı-mod eklendi). Önce oku.
1. Bu dosyayı oku. Kurallar: ÖLÇ-ÖNCE, kalite-güvenli, reversibilite, cross-dataset.

---

## 1. BU OTURUMDA YAPILAN — Akıllı mod seçimi (commit `c89041c`, ONAYLI plan)

**Hedef:** Kullanıcı Ultraplan'da "akıllı NFV/heightmap mod seçimi"ni onaylamıştı; PR gelmedi → yerel uygulandı.
**Kullanıcı şartı:** "Yanlış SEÇMEMELİ, kalite DÜŞMEMELİ — kanıtla."

### Çözüm: `predict_nfv_benefit` (src/nesting3d/adaptive_params.py)
- **VARSAYILAN NFV** (kalite-güvenli — K-12: NFV >= heightmap HER ZAMAN; cavity yoksa birebir eşit).
- **heightmap SADECE** NFV'nin %0-kazanç + boşuna-yavaş olduğu 2 BARİZ durumda → kazan-kazan:
  - (A) NET KUTU: `mean_aspect_z < 4.0` (cavity yok). boxy: 2.8; plan'lar 6.4+ (geniş marj).
  - (B) İNCE-PLAKA dom.: `thin_plate_ratio > 0.6` (düz-optimal, NFV eğmek uzatır, K-15). numune: 0.92.
- **Kalite-riski asimetrisi:** NFV yanlış-pozitif = SADECE hız (kalite-güvenli); heightmap yanlış-negatif
  (cavity-zengin→heightmap) = %14-29 KALİTE KAYBI → bu yüzden ŞÜPHEDE NFV → **false-negative=0**.
- Eşikler mekanizma-türevli (K-14/K-15 height-driver formu) + geniş güvenlik marjı (sabit-uydurma DEĞİL).

### KANIT (kullanıcının "kanıtla" şartı)
- **ÖLÇ-ÖNCE** (`scripts/automode_proof.py`, kalıcı): 5 gerçek veride feature + bilinen kazanç →
  plan1/2/3 (kazanç 14/20/29%) → **nfv**; numune/boxy (0%) → heightmap. **false-negative=0**
  (cavity-zengin hiçbir veri heightmap'e gitmedi). Ayrıştırıcı: `mean_aspect_z` ORTA bant (6-13) =
  NFV-kazanır; düşük=kutu, çok-yüksek=ince-plaka = kazanmaz.
- **Birim test** (`tests/test_adaptive_params.py`, 6 test): false-negative koruması kalıcı kapı.
- Güvenlik teoremi (K-12): NFV >= heightmap → NFV seçimi asla kaliteden kaybettirmez.

### Bağlama
- `scripts/demo_pipeline.py::_process_batch`: `nesting_mode=="auto"` → predict_nfv_benefit (instance'tan,
  voxelize'sız ucuz) → mod; **DEFAULT auto** (550 + payload 1113). Çıktıya şeffaf `nesting_mode_used` +
  `auto_mode_reason` (hangi mod neden). Güvenli düşüş: auto başarısız → heightmap.
- `src/webapp/app.py` /run + /otonom: auto/nfv/heightmap, default auto.
- `src/webapp/templates/index.html`: checkbox → **3'lü radio** (Otomatik[default]/NFV/Heightmap) + JS.
- Webapp kontrat testleri yeni davranışa güncellendi (checkbox→radio, default→auto).
- **Tam suite 2078 passed** (regresyon yok; kutu fixture auto→heightmap = eski davranış birebir).

---

## 2. AÇIK / SONRAKİ İŞLER
### Görev #5 — SÜRE DERİNLEŞTİRME (açık, AYRI/RİSKLİ) ⚠️
Çift-voxelize (H-13) sonrası kalan: `coarse_to_fine` FINE adımı büyük parçayı 0.5mm voxelize (159s/parça
`_surface_cells`). Kök = pitch R6 (tek 1mm parça → 356mm parça da 0.5mm). Çözüm riskli (parça-kaybı +
H-06 duvarı + cross-dataset). ÖNCE çift-voxelize sonrası Plan2 gerçek süre ÖLÇ. Detay `RESUME_2026-06-26 §2`.

### İleride
- Akıllı-mod ML yükseltme: telemetri birikince (her işin NFV-vs-heightmap kazancı loglanır) selection/
  altyapısıyla öğrenen sürüm. Şimdilik açıklanabilir-kural yeterli + kanıtlı.
- Magics %6 kalite açığı = SÜPER BİLGİSAYAR (A1 phi-function), `RESUME_2026-06-25.md`.

---

## 3. OPERASYONEL NOTLAR
- cp1254 stdout: emoji/sembol (→ ✓ ✅) print çökertir → ASCII-safe ([[feedback-windows-stdout-ascii]]).
- `extract_features` STL bbox'larından türer (build_instance_from_order doldurur; box+STL çalışır).
- predict_nfv_benefit voxelize GEREKTİRMEZ (sadece bbox feature) → ucuz, pipeline başında çalışır.
- Veri: `Veriler/Plan{1,2,3}/` + numune `NUMUNE_DIR` (STL stem "1".."8" → qty "n"+stem eşleştir).

## 4. DOSYALAR (bu oturum, commit `c89041c`)
- `src/nesting3d/adaptive_params.py` (predict_nfv_benefit + ModeDecision)
- `scripts/demo_pipeline.py` (auto bağlama + default + şeffaf çıktı)
- `src/webapp/{app.py, templates/index.html}` (auto + radio)
- `tests/{test_adaptive_params.py(yeni), test_demo_pipeline.py, test_webapp_orders.py, test_webapp_otonom.py}`
- `scripts/automode_proof.py` (5-veri false-negative=0 kalıcı kanıtı)
- Önceki handoff `RESUME_2026-06-26.md`; karar veritabanı `MOTOR/YONTEM_HARITASI_DATA_BASE.md` (K-16).
