# RESUME — Yeni Session Buradan Başla (2026-06-22)

> Magics nesting açığını kapatma çalışması. Bir önceki session'ın (2026-06-21)
> tam handoff'u. Branch: `app-demo-build`. Git TEMİZ değil — değişiklikler
> commit'siz (aşağıda), kasıtlı (kullanıcı sabah diff kontrol edecek).

---

## 0. İLK İŞ (yeni session)

1. Bu dosyayı + `[[project-kiyas-iyilestirme]]` memory'sini oku.
2. **✅ drop_map vektörizasyonu BİTTİ (2026-06-22)** — aşağıdaki §0.1.
3. **Sıradaki somut iş:** Plan2 KESİN yeniden profil (drop_map çözüldü; şimdi
   darboğaz fine voxelizasyon mu? — Açık Soru 1, §8) VEYA Faz 2 fine-açı rotasyon.
4. Koda başlamadan önce §5 "DİKKAT" kurallarını oku (kalite koruma + commit yok).

---

## 0.2. ✅ FAZ 2 (kalite) + ADAPTİF KATMAN — TAMAMLANDI (2026-06-22)

**n=8 pozlar:** `n_orientations` `coarse_to_fine`'a baştan sona bağlandı (önceden
harness bayrağı ölüydü). Plan1: **n=4→132, n=8→117 (%11.4 ✅), n=12/28→117 (yok).**

**İnce-açı refinement (Faz 2b — hocanın 0.5-1° isteği):** `voxelize_part(rot_matrices=)`
keyfi açı; `_build_refined_fine_parts` (model,poz) önbellekli; kazanan pozun ±5°/1°.
**Ham greedy Plan1'i BOZDU (117→136.5):** yerel z_top ≠ global yükseklik.
→ **GÜVENLİ MOD (varsayılan):** algoritma açısız+açılı çözümü üretir, global iyiyi
seçer → ince açı ASLA zarar veremez. `fine_angle_used` şeffaflık.

**ADAPTİF (`adaptive_params.py`):** "kutuluk" özelliğinden ince-açıyı otomatik seç
(kutu→atla, düzensiz→aç). Eşik 0.85. `solve_coarse_to_fine(adaptive=True)` + `--adaptive`.

**Plan2 n=8 hızlı: 637.5/1.295×/91dk** — UYARI hızlı mod, eski 621 KESİN ile kıyaslanamaz;
n=4 hızlı baseline KOŞULUYOR (kazanç izolasyonu). 91dk → n=8 sabit yapma, poz da adaptif olmalı.

**STRATEJİK:** kalite açığı rotasyon DEĞİL (eğim+ince açı yardım etmedi) → **Faz 4 (cavity) asıl iş.**
**Test:** coarse_to_fine 30/30, bin3d 225/225. **COMMİT'SİZ.**
Dosyalar: bin3d.py, coarse_to_fine.py, voxelize.py, adaptive_params.py(YENİ), kiyas_harness.py, 2 test.

**SIRADAKİ KOD:** (1) adaptif katmanı POZ SAYISINA genişlet (coarse-escalation: n=4→8
coarse'ta dene, kazanç yoksa dur, fine bir kez). (2) Faz 4 cavity-aware.

## 0.3. ✅ LOKAL AI (kişisel, nesting'den bağımsız) — KURULDU (2026-06-22)
Open WebUI(Docker:3000)+Ollama(D:\ollama)+Tailscale(uzaktan 100.102.113.89)+RAG.
Detay: `[[project-lokal-ai-kurulum]]` memory + masaüstü `open-webui-giris.txt`.

## 0.1. ✅ drop_map vektörizasyonu — TAMAMLANDI (2026-06-22)

**Ne yapıldı:** `bin3d.py` genel yolu (konkav footprint / değişken taban — gerçek
STL) Python `for` döngüsünden `sliding_window_view` tek-redüksiyona çevrildi.
- `drop_map` → `_drop_map_fast` (kutu, değişmedi) → `_drop_map_general` (YENİ vektörize).
- `_drop_map_loop` = eski döngü, yalnız devasa-footprint bellek fallback'i (CAP 64MB).
- `max` birleşmeli + tamsayı → sonuç döngüyle **BİREBİR AYNI** (kalite garantisi).
- Bellek: pencere kopyası x-ekseninde bloklara bölünür.

**Doğrulama:**
- Test: `tests/test_bin3d_dropmap.py` — yeni `TestDropMapGeneralVectorizedEquivalence`
  (konkav + değişken taban + z_clearance + x-blok, `np.array_equal`). **225/225 geçti.**
- Harness: Plan1 hızlı (seed 42, fine_pitch 1.5) — yükseklik **132.0→132.0 (AYNI)**,
  doluluk %15.5 (aynı), süre **77.9s→55.7s (%28.5 hız)**. CSV tag `faz1-dropmap-vektorize`.

**Commit'siz** (RESUME §5 kuralı — kullanıcı sabah diff inceleyecek). Değişen dosyalar:
`src/nesting3d/bin3d.py`, `tests/test_bin3d_dropmap.py`, `results/kiyas_sonuclari.csv`.

---

## 1. BÜYÜK RESİM — neredeyiz

Bizim 3B nesting motoru Magics'in gerisinde: **kalite** (Plan2: bizim 621mm vs
hoca 492.39mm = **1.26×**, doluluk %8) ve **hız** (Plan2 ~**78dk**). Bunları
kapatmak için: araştırma → yol haritası → uygulama. Araştırma + yol haritası BİTTİ,
uygulamada **Faz 1 (hız)** ortasındayız.

**Yol haritası (repo kökü `PLAN_IYILESTIRME_YOLHARITASI.md`) — 5 faz:**
- Faz 0: ölçüm altyapısı — **✅ BİTTİ**
- Faz 1: HIZ (per-part/octree/drop_map) — **🔄 DEVAM** (tanı bitti, çözüm netleşti)
- Faz 2: KALİTE fine-angle rotasyon — bekliyor
- Faz 3: KALİTE NLP compaction (quasi-phi) — bekliyor
- Faz 4: KALİTE cavity-aware/gerçek-3B — bekliyor

---

## 2. ÜRETİLEN KALICI ÇIKTILAR (duruyor, kaybolmadı)

- **Araştırma arşivi:** `MOTOR/makaleler/02_magics_kiyas_literatur_2026-06-21/`
  → `SENTEZ_RAPORU.md` (5 alan analiz), `KAYNAKLAR.md` (22 künye + DOI), `pdf/`
  (3 açık-erişim PDF: EJOR 2018 quasi-phi, SFF 2015 Araujo, NVIDIA 2010 octree).
  Hocaya gösterilebilir. (Ayrıca repo kökü `ARASTIRMA_NESTING_LITERATUR.md` kopya.)
- **Yol haritası:** `PLAN_IYILESTIRME_YOLHARITASI.md` (repo kökü).
- **Ölçüm harness'ı (Faz 0):** `scripts/kiyas_harness.py` — Plan1/Plan2'yi sabit
  tohum + sabit plakada kesin/hızlı modda koşar, `results/kiyas_sonuclari.csv`'ye
  append eder. Kullanım:
  `PYTHONIOENCODING=utf-8 python -u scripts/kiyas_harness.py --plan plan1 --mode hizli --tag X`
  - `--mode kesin`: tam pipeline (run_pipeline), auto pitch. Plan2 ~78dk.
  - `--mode hizli`: solve_coarse_to_fine doğrudan, `--fine-pitch 1.5` override, dk'lar.

---

## 3. FAZ 1 — NE DENENDİ, NE ÖĞRENİLDİ (3 önemli ders)

### Ders 1 — pitch tabanlı 1a YANLIŞ (geri alındı)
"Aykırı ince parçaya az voxel ver" (suggest_pitch'i değiştir) denendi. Tek bin
grid'inde (bin3d parça↔bin AYNI pitch, `drop_map` indeks hizalaması) pitch'i
kabalaştırmak **TÜM parçaları kabalaştırıp** Plan1'i 106.7→129.5mm (**%21**) bozdu.
→ `pitch.py` HEAD'e geri alındı, **DOKUNMA**. Kullanıcı: "kalite çok önemli".

### Ders 2 — coarse arama = KALİTE motoru
`coarse_to_fine`: coarse aşaması ARAR (sıra+oryantasyon bulur), fine aşaması SADECE
o kararı ince çözünürlükte yerleştirir (arama YOK, `place_in_order` tek geçiş).
→ coarse budget/pitch'i kısmak doğrudan **kaliteyi düşürür**. coarse_pitch süpürmesi
(×3..×8) yapıldı: yükseklik düzensiz (×3,×8 iyi=106.7; ×4,×5,×6 kötü=112-114), süre
gürültülü. **coarse_pitch güvenilir kaldıraç DEĞİL.**

### Ders 3 — PROFİL: gerçek darboğaz `drop_map` (%72) ✅ KESİN
cProfile (Plan1, coarse arama):
```
bin3d.py:54 drop_map → tottime 150s / 209s = %72  (79.296 çağrı)
```
Darboğaz `drop_map`'in **konkav-footprint genel döngüsü** (`bin3d.py:74-78`: dolu
kolonlar üzerinde Python `for` döngüsü). Gerçek STL parçaları konkav → mevcut
**fast path** (`_drop_map_fast`, satır 83) "filled tamamen dolu kutu" şartını
sağlayamıyor → yavaş genel yola düşüyor.
**Bu OCTREE ile çözülmez** (octree çarpışma içindir; buradaki iş heightmap drop
hesabı). Doğru araç: drop_map vektörizasyonu.

---

## 4. SIRADAKİ SOMUT İŞ — drop_map vektörizasyonu

**Hedef:** `drop_map` genel yolunu hızlandır, **yükseklik BİREBİR AYNI kalsın**
(kalite garanti). Ortam: **numba YOK**, **scipy.ndimage VAR** (scipy 1.17.1).

**drop_map matematiği** (`bin3d.py:54`):
```
Z[x,y] = max over filled columns (i,j) of  H[x+i, y+j] - bottom[i,j]
         (sonra max(Z,0); z_clearance varsa Z>0 olanlara ekle)
```
- Mevcut fast path (`_drop_map_fast`): filled.all() AND bottom tek-değer → ayrılabilir
  kaydırma (O((fw+fh)·npx·npy)). Sadece KUTU parçalar.
- Genel yol: K=dolu_kolon iterasyon, her biri (npx,npy) numpy slice+max. K büyük → yavaş.

**Önerilen çözüm (kademeli, en güvenliden):**
1. **Gevşetilmiş fast path — "düz taban":** filled konkav olsa bile `bottom` tek-değer
   (b0) ise (gerçek parçaların ÇOĞU düz oturur), `scipy.ndimage.maximum_filter`
   VEYA footprint-maskeli sliding-window-max ile genel döngüden kurtul. Dikkat:
   maximum_filter 'same' boyut + merkez-hizalı; drop_map "valid" (npx=nx-fw+1) +
   sol-hizalı istiyor → kırpma/offset DOĞRU yapılmalı (yoksa yanlış yükseklik).
2. Değişken bottom kalan durumlar için: bottom değerine göre grupla, her grup için
   tek footprint-max; veya genel döngüde kal (az parça).

**KALİTE KORUMA TESTİ (zorunlu, kod yazmadan önce yaz):** yeni drop_map çıktısı eski
`drop_map` ile **birebir aynı** olmalı — rastgele orientation + rastgele heightmap
üzerinde `np.array_equal(yeni, eski)`. Mevcut `_drop_map_fast` zaten genel yolla
birebir aynı olacak şekilde yazılmış (satır 92-94 notu) — aynı disiplin.

**Doğrulama:** `scripts/kiyas_harness.py --plan plan1 --mode hizli` önce/sonra:
yükseklik AYNI (106.7 civarı), süre düşmeli. Sonra Plan2 kesin (~78dk→hedef ≤15dk).

---

## 5. DİKKAT (kullanıcı kuralları)

- **KALİTE > HER ŞEY.** Yükseklik bozan hiçbir "hız" değişikliği kabul DEĞİL.
  Her hız işi birebir-aynı-sonuç testinden geçmeli.
- **git commit / push YAPMA.** Kullanıcı sabah diff'i kendisi inceleyecek.
  Şu an commit'siz değişiklikler: `MOTOR/makaleler/00_OKUMA_LISTESI.md` (M),
  yeni dosyalar (ARASTIRMA_*, PLAN_IYILESTIRME_*, RESUME_*, scripts/kiyas_harness.py,
  results/kiyas_sonuclari.csv, MOTOR/makaleler/02_*). `pitch.py` = HEAD (temiz).
  `.claude/settings.local.json` DEĞİŞMEDİ (izin eklenemedi — aşağıda).
- **İZİN:** Claude kendi onay izinlerini settings'e ekleyemiyor (auto-mode classifier
  "self-modification" diye engelliyor — denendi, 2× reddedildi). Tam otonom için
  KULLANICI: ya `.claude/settings.local.json` `allow` dizisine git/rm/cp/mv/tee/...
  kurallarını elle eklesin, ya `claude --dangerously-skip-permissions` ile başlatsın.
  (Mevcut settings'te `Bash(python *)`, `Bash(mkdir *)` zaten var → python/test koşuları
  onaysız çalışıyor.)
- pitch.py'ye DOKUNMA (Ders 1). coarse budget/pitch'i kalite için kısma (Ders 2).

---

## 6. KIYAS REFERANS RAKAMLARI

| Plan | Bizim (baseline) | Hoca (Magics) | Oran | Plaka | Süre |
|---|---|---|---|---|---|
| Plan1 | 106.7mm (doluluk %16.6) | YOK | — | auto 340×340 | coarse ~190s |
| Plan2 | **621.0mm** (doluluk %8) | **492.39mm** | **1.26×** | 328.74×328.19 | **~78dk** |
| Plan3 | 685mm | 593.86mm | 1.15× | — | (eski not) |

Hoca verisi: `C:\Users\erenk\OneDrive\Masaüstü\Veriler\Plan{1,2,3}\Plan{1,2,3}\*.stl`
Adetler: `scripts/kiyas_harness.py` PLANS dict'inde gömülü.

---

## 7. MOTOR MİMARİSİ (hızlı referans, file:line)

- `voxelize.py:60 rotation_matrices(n_orientations=4)` — 28 poz (0-7 eksen, 8-11 eğik
  Rx 55-70°, 12-27 simetri). Kıyas n_orientations=4 → saf 90°, eğim KAPALI. (Faz 2.)
- `bin3d.py:54 drop_map` — heightmap drop (DARBOĞAZ). `:83 _drop_map_fast` (kutu).
  `:132 place`. Heightmap 2B `height[x,y]`, tek pitch.
- `dblf.py:28 _best_position` (lex-min z_top,z,y,x), `:50 place_in_order`, `:166 dblf`.
- `sa3d.py:30 Solution=List[(part,orient_idx)]`, `:44 _energy=max_h+0.1*rms`,
  `:49 _neighbour` (swap/insert/2opt/flip).
- `coarse_to_fine.py:140 solve_coarse_to_fine` (coarse ara → fine yerleştir).
  `CoarseToFineResult`: height_mm, density, coarse_time_s, fine_time_s, n_placed.
- `instances/pitch.py:56 suggest_pitch = min_feature/2.5` (GLOBAL, floor 0.5). DOKUNMA.
- `scripts/demo_pipeline.py:924 run_pipeline`; nesting çekirdeği `_run_one_batch`
  (~satır 537+, C2F_THRESHOLD üstü→coarse_to_fine, altı→tuner). pitch satır 576.
- Solverler: `solvers/{dblf,sa,ga,tabu,alns}_solver.py`, `tuner.py:187 tune`.
- Bağımlılık: numpy, trimesh, shapely, scipy (ndimage VAR), matplotlib. **numba YOK.**

---

## 8. AÇIK SORULAR (araştırma raporundan, ileride)

1. Plan2 gerçek coarse/fine zaman ayrımı ölçülmedi (78dk; Plan1 tanısı coarse %81
   ama Plan2'de 0.5mm/270M voxel fine voxelizasyon ağır olabilir — drop_map çözülünce
   tekrar profil al).
2. Faz 3 NLP (quasi-phi) 226 parça ölçeğinde compaction ile çözülür mü (literatür N=15-40).
3. Cavity-aware (Faz 4) tek başına yüksekliği ne kadar düşürür.
