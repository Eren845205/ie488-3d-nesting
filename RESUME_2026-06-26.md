# RESUME / HANDOFF — 2026-06-26 (oturum 2) — yeni session buradan başla

> **EN GÜNCEL handoff.** Bu oturum APP UÇTAN-UCA akışına (mail→parse→nesting) ve nesting
> ROBUSTLUK/HIZ'a odaklandı (kalite değil). Önceki: `RESUME_2026-06-25.md` (Magics açığı + literatür).
> Branch `m1-cavity-nfv`, git **TEMİZ**. Bu oturum 3 commit: `36b5537`, `36744b3`, `10b5f2e`.
> Rollback tag'leri: `checkpoint-2026-06-26-pre-{mailfix,oom,doublevox}` + `checkpoint-2026-06-22-faz1-2`.

---

## 0. İLK İŞ (yeni session)
0. **📊 KARAR VERİTABANI** `MOTOR/YONTEM_HARITASI_DATA_BASE.md` = nesting motoru tek doğruluk kaynağı
   (kalite K-xx / hız H-xx). Bu oturum H-12 (OOM-chunk) + H-13 (çift-voxelize) eklendi. Önce onu oku.
1. Bu dosyayı oku. App akışı (mail/parse) bu oturumun yeni alanı → aşağıda detay.
2. Kurallar: ÖLÇ-ÖNCE, birebirlik kapısı (kaliteyi-bozmayan değişiklik), reversibilite, cross-dataset.

---

## 1. BU OTURUMDA YAPILAN (hepsi COMMIT'Lİ + canlı doğrulandı)

### İş 1 — Mail-çekme filtreleme bug'ı DÜZELTİLDİ (commit `36b5537`)
**Kullanıcı sorusu:** "Otonom pipeline mailden veri çekiyor mu? Birincil maillere bakmalı, diğerlerine bakıyor."
- **BUG (kanıtlandı):** `src/runtime/mail_ingest.py::ImapMailbox._fetch_uids` Gmail INBOX'ı düz `SEARCH ALL`
  + son `max_fetch=20` ile tarıyordu → Gmail kategorilerini (Primary/Promotions) AYIRT ETMİYOR →
  reklamlar (LinkedIn/Google) son-20 penceresini doldurup gerçek sipariş maillerini (Primary'de, ZIP-STL
  ekli) dışarı itiyordu. İkincil kök: `make_mail_source` provider'ı `pop` ediyordu → Gmail filtresi çalışmazdı.
- **FIX:** provider korunuyor + opt-in `category_filter`/`prefer_attachments` config alanları + `_fetch_uids`
  provider-aware Gmail `X-GM-RAW "category:primary OR has:attachment"` (Outlook → try/except düz ALL fallback).
  `configs/mail.local.json`'a `category_filter:primary`+`prefer_attachments:true` eklendi. 4 yeni test.
- **CANLI:** Düzeltilmiş üretim `fetch_new` artık `Plan1.zip`/`Plan3.zip` (hocadan FW'lenmiş, INBOX'ta mevcut)
  sipariş maillerini çekiyor (önceden pencereden düşüyordu). App `/otonom` otomatik kullanıyor
  (`_resolve_mail_source_config` json'u olduğu gibi okuyor — app.py:308).

### İş 2 — Voxelize OOM DÜZELTİLDİ (commit `36744b3`)
**Sorun:** Plan2 gerçek veriyle `run_pipeline` (default heightmap) ÇÖKÜYORDU — `to_voxel_parts` 4.54 GiB
istiyordu → `height=0` BAŞARISIZ ("olmuyordu" derdi).
- **Kök (teşhisle):** `_surface_cells` (voxelize.py) büyük DÜZ yüzeyleri (356mm dev parça P155308 tabanı,
  az ama dev üçgen) ince 0.5mm pitch'te tek-seferde barycentric örnekliyordu → `pts (363, 559153, 3)` =
  **4.87GB tek array** OOM. (k_per_tri ~1056, n_bary ~5.6e5).
- **FIX:** `_surface_cells` mk-chunk'lama (`_PTS_CHUNK_ELEMS=8M`, ~0.19GB/chunk). **BİREBİR aynı grid**
  (chunk==referans doğrulandı: 194914=194914 voxel; `_mark` idempotent). Pitch politikasına dokunulmadı.
- **Sonuç:** 356mm parça artık OOM'suz voxelize oluyor. Kritik testler 98 passed.

### İş 3 — Çift-voxelize KALDIRILDI (commit `10b5f2e`)
**Süre kökünün bir parçası:** `_process_batch:610` HER ZAMAN fine voxelize ediyordu ama NFV (`solve_nfv`)
ve coarse_to_fine KENDİ voxelize'larını yapar → o yollarda BOŞA (Plan2 büyük parça @0.5mm = 159s/parça çift).
- **FIX:** `voxel_parts` SADECE tuner + DBLF-fallback yolunda üretiliyor. Yol kararı (`>C2F_THRESHOLD=40`)
  voxelize ETMEDEN `estimated_n_parts = sum(qty) by distinct name` ile (to_voxel_parts semantiği birebir).
  Voxelize hata yakalama tuner yolunda korundu; DBLF fallback None-güvenli.
- **BİREBİRLİK (git stash öncesi=sonrası):** 45-parça coarse yolu height=36.0 + density AYNI; süre
  7.0s→4.4s (%37). Kalıcı regresyon testi eklendi. 175 test passed.

### Yan doğrulama — ZIP-STL parse uçtan-uca ÇALIŞIYOR (LLM'siz)
`Plan1.zip` maili → `ingest_order` → ZIP açıldı → **12 STL çıkarıldı → adet (gövdeden) + bbox eşleşti**
(811791-1 qty=19, baseplate_v2 qty=1, TAPER-GAUGE-1 qty=10, ... toplam ~112 adet). `needs_review=None`,
hiçbir parça atlanmadı. **LLM bu yolda YOK** (deterministik); LLM yalnız serbest-metin parse + açıklama/teklif.

---

## 2. AÇIK / SONRAKİ İŞLER (öncelik sıralı)

### Görev #5 — SÜRE DERİNLEŞTİRME (açık, AYRI/RİSKLİ) ⚠️
Çift-voxelize YARI çözümdü. Kalan darboğaz: `coarse_to_fine` FINE adımı büyük parçayı yine 0.5mm'de
voxelize ediyor (159s/parça `_surface_cells`). **Kök = pitch 0.5mm çok ince** (tek 1mm parça yüzünden
356mm parça da 0.5mm — `instances/pitch.py::suggest_pitch = min_feature/2.5`, **R6 sorunu**). Çözüm RİSKLİ:
(a) `_surface_cells` algoritma hızı, (b) pitch politikası (parça-kaybı + **H-06 duvarı** "tek grid pitch
ayrılamıyor" + cross-dataset kalite). Kendi plan modunu hak ediyor. **ÖNCE: çift-voxelize sonrası gerçek
Plan2 süresini ÖLÇ** (kesin baz, ÖLÇ-ÖNCE) — uzun olabilir, background tek-süreç.

### Görev #2 — Nesting AKILLI-MOD seçimi (Ultraplan'da, PR bekleniyor)
NFV-cavity vs heightmap'i veriye göre OTOMATİK seç. **Ultraplan'a (web) gönderildi + ONAYLANDI**, PR olarak
gelecek. Plan: kutuluk tek-ölçüt YETMİYOR (numune 0.35→%0 aykırı, K-12) → açıklanabilir geometrik kural
(height-driver formu: istif-zorunlu-blok vs düz-optimal-ince-plaka), mevcut 20-feature reuse, default `auto`.
Geldiğinde entegre + cross-dataset doğrula.

### Düşük öncelik
- Kalan KALİTE (Magics %6 açığı) = SÜPER BİLGİSAYAR (A1 phi-function), `RESUME_2026-06-25.md` geçerli.

---

## 3. OPERASYONEL NOTLAR
- Ağır ölçümler (Plan2 nesting) TEK SÜREÇ + `> log 2>&1`. Kullanıcı uzun background'ları durdurabiliyor →
  küçük/sentetik testlerle (45-parça coarse) hızlı birebirlik kanıtı tercih et.
- cp1254 stdout: emoji/sembol print çökertir ([[feedback-windows-stdout-ascii]]) → ASCII-safe (`encode('ascii','replace')`).
- `mail.local.json` git-IGNORED (şifre) — değiştirirken Python json ile (ekrana basmadan).
- Veri: `Veriler/Plan{1,2,3}/`, loader `scripts/c3_generality.py` DATASETS + `build_instance_from_order`.
- Ollama çalışıyor (qwen2.5-coder:7b); Gmail config `erenkutluu05@gmail.com` (app-password).

## 4. DOSYALAR (bu oturum)
- `src/runtime/mail_ingest.py` (X-GM-RAW filtre) + `configs/mail.{local,example}.json` + `tests/test_mail_ingest.py`.
- `src/nesting3d/voxelize.py` (_surface_cells chunk).
- `scripts/demo_pipeline.py` (_process_batch çift-voxelize) + `tests/test_demo_pipeline.py`.
- Önceki handoff: `RESUME_2026-06-25.md`. Karar veritabanı: `MOTOR/YONTEM_HARITASI_DATA_BASE.md` (H-12/H-13).
