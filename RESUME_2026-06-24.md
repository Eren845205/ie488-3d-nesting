# RESUME / HANDOFF — 2026-06-24 (yeni session buradan)

> **AMAÇ:** Magics kalite açığını kapatma — NFV cavity "kalite modu". Bu dosya BUGÜNÜN
> oturumunu kapsar; önceki bağlam `RESUME_2026-06-23.md` (§11-12 NFV üretime bağlama temeli) +
> `[[project-kiyas-iyilestirme]]` + `[[project-nfv-sonraki-oturum-backlog]]` memory.
> Branch `m1-cavity-nfv`, git **TEMİZ**. ROLLBACK: `git reset --hard checkpoint-2026-06-22-faz1-2`.

---

## 0. İLK İŞ (yeni session)
1. Bu dosyayı + `[[project-nfv-sonraki-oturum-backlog]]` (madde 1-6 durumları) oku.
2. **NFV opt-in** — günlük akış default HEIGHTMAP; NFV sadece UI checkbox seçilince devrede.
3. Kurallar: KALİTE>HER ŞEY, **SABİT-SAYI YASAK**, **ÖLÇ-ÖNCE** (deney → ölç → sonra uygula),
   reversibilite. [[feedback-windows-stdout-ascii]] (deney scriptlerinde ASCII print).

---

## 1. BU OTURUMDA YAPILAN (hepsi COMMIT'Lİ, git temiz) — 8 commit
NFV "kalite modu" çekirdeği baştan sona kuruldu + UI'a bağlandı. Backlog #1-#4 + #6 tamamlandı.

| İş | Sonuç | Commit |
|---|---|---|
| NFV-farkında adaptif pitch + bellek guard | suggest_nfv_pitch (Plan2 0.5mm/OOM → 2.0mm/112s) | `2d8a5da` |
| UI mod seçici (manuel /run) | "Kalite modu (NFV)" checkbox | `75d2f5b` |
| Otonom akış NFV (eksik halka) | "Mailleri Çek" akışına checkbox | `2354a51` |
| FastCPU (pyfftw) backend | DENENDİ → NO-GO (5.3× yavaş, plan-overhead) | `a57dbac` |
| pitch cross-dataset genelleme | Plan2-overfit giderildi (Plan1 çökmesi düzeldi) | `a26d180` |
| pitch plaka-oranı guard | boxy sığmama çökmesi (max_parça+margin plakaya sığmalı) | `621936c` |
| **oryantasyon n=8 default + quality=max** | adaptif-DBLF NO-GO; n=8 sweet spot | `ae306c3` |
| **"Maksimum kalite" UI seçeneği** | quality=max checkbox (/run + otonom) | `d835458` |

---

### DOĞRULAMA / TEST DURUMU (handoff)
Bu oturumda değişen dosyaların testleri AYRI AYRI YEŞİL: NFV `test_nfv_solve` (10), pitch
`test_instances_pitch` (22), webapp `TestNestingModeOptIn`+`TestOtonomNestingMode` (11), demo_pipeline
entegre (41). Full regresyon %90+ passed (hiç F yok); ⚠️ bu MAKİNE RAM darlığından (16GB, ~2-3GB boş +
cupy GPU testleri) full suite SONUNDA native crash edebiliyor (kod değil, ortam) → GPU-ağır testler
(`test_nfv_solve`, `test_parallel_decode`, `test_webapp_otonom`) AYRI koşuldu. Bol-RAM makinede tek
seferde `pytest -q` temiz geçer. Önceki baz: 2057 test (RESUME_06-24 öncesi commitlerde yeşildi).

## 2. EN ÖNEMLİ ÖLÇÜMLER + KARARLAR (bu oturum)

### PITCH (veri+donanım odaklı, SABİT DEĞİL) — `src/nesting3d/instances/pitch.py`
`suggest_nfv_pitch`: güvenli oran 1.0'dan (parça-garanti voxelize) başla → bellek aşarsa voxelize-
sınırına (0.5) kadar KABALAŞTIR → plaka-oranı tavanı (en büyük parça+2·margin sığmalı) → sığmazsa
feasible=False → heightmap'e düş. Hem min_feature (geometri) hem RAM hem plaka'dan türer.
Doğrulandı: plan1→2.54, plan2→1.77, plan3→2.5, boxy→20 (hiçbiri çökmez).

### ORYANTASYON (n) — `src/nesting3d/nfv_solve.py` (ÖLÇÜM `scripts/c3_quality_levers.py`)
Plan2 @2.0mm GPU: n=4=556, **n=8=522 (+%6.1)**, n=12=516 (+%1.1 AMA 3× yavaş 122→362s), n=28 OOM.
Plan1: n=8 +%3.8 (rakamlar VERİYE göre değişir, mantık aynı).
- **Multi-start (sıra) = ÖLÜ:** jitter sıraları birebir 556 (largest-first zaten optimal). SA gibi NO-GO.
- **Adaptif DBLF-prob = NO-GO:** DBLF NFV n-getiriyi TERS tahmin (Plan2 trail n=4:639<n=8:645 dedi ama
  NFV'de n=8 İYİ) + 919s yavaş. DBLF heightmap NFV-cavity'yi TEMSİL ETMEZ.
- **KARAR: n=8 SABİT default** (`NFV_DEFAULT_ORIENTATIONS=8`). NEDEN overfit değil: 4⊂8 küme-içerme →
  NFV greedy n=8'de en az n=4 kadar iyi → kalite HER veride garantili ≥ n=4. Hız ~1.5×.
- **quality="max":** `_hw_max_orientations` RAM-tavanı (16GB→12, datacenter→28). `scenario["nfv_quality"]="max"`.

### MAGICS DURUM
| | mm | not |
|---|---|---|
| Magics Plan2 | 492 | 0.5mm fine, hedef |
| NFV n=4 (eski) | 556 | %13 geride |
| **NFV n=8** | **522** | **%6 geride** (kaba 2.0mm) |
⚠️ Kaba pitch (2.0) vs Magics fine (0.5) — adil değil. **Süper bilgisayar = n=28 + 0.5mm fine** (bizde
OOM: n=12 zorlandı, n=28/0.5mm sığmaz) → NFV gerçek tavanı + adil kıyas. Kalan fark pitch + algoritma.

---

## 3. UYGULAMA AKIŞI (nasıl kullanılır)
- `scenario["nesting_mode"]="nfv"` → NFV cavity (default heightmap). `"nfv_quality"="max"` → donanım-tavanı.
- UI: webapp ana sayfa "Kalite modu (NFV cavity)" checkbox + altında "Maksimum kalite" (her iki akış:
  /run formu + "Mailleri Çek ve İşle" otonom kartı). `NFV_BACKEND=scipy` GPU'yu zorla kapatır.
- 3 dispatch noktası demo_pipeline `_process_batch` + payload + `run_pipeline`; app.py /run + /otonom.

## 4. KALAN İŞLER (düşük öncelik)
- **5b:** app KURULUM SİHİRBAZI'na cupy oto-kurulum ([[project-app-kurulum-gpu-cupy]]) — DAĞITIM-ZAMANI
  işi (web-sunucu modeli; Electron REDDEDİLDİ=çok-istemci [[project-dagitim-topolojisi]]). Şu an sihirbaz
  YOK (sadece `Konteyner_Nesting_Baslat.bat` başlatıcı + requirements.txt elle pip).
- **SÜPER BİLGİSAYAR:** Plan2 n=28 + 0.5mm fine pitch koş → NFV gerçek tavanı + Magics adil kıyas.
- **3 tam hız tablosu** zaten alındı (plan1 +%10.3/GPU 2.7×, plan3 +%20.2/GPU 3.3×).

## 5. SCRIPTLER (bu oturum, hepsi DENEY scripts/)
`c3_pitch_curve.py` (pitch eğrisi), `c3_xdataset_speed.py` (cross-dataset hız), `c3_fastcpu_bench.py`
(pyfftw NO-GO), `c3_quality_levers.py` (n=8/multi-start ölçümü). `src/` üretim: pitch.py + nfv_solve.py.

## 6. OPERASYONEL NOT (önemli — zaman kaybettirdi)
Bu ortamda **background bash + `grep`/`tee` pipe ÇIKTI YUTUYOR** (python biter, log boş kalır). Güvenilir
yöntem: **foreground** VEYA `> dosya 2>&1` redirect (grep YOK). Ağır NFV deneyleri tek-süreç koş (paralel
GPU işleri RAM/VRAM çakışıp ikisi de çöker). [[feedback-windows-stdout-ascii]] = cp1254 sembol tuzağı.

---

## 7. OTURUM 2 (aynı gün, 06-24) — NFV KALİTEYİ-BOZMAYAN HIZ ARAŞTIRMASI → (A) NO-GO
**Kullanıcı sorusu:** "NFV kaliteden ödün vermeden DAHA hızlanabilir mi + GENEL mi (overfit yok)?"
Ek işler (git temiz, 3 commit `459e343`, `0753df6` + UI doğrulama oturumu):

**UI doğrulama (Playwright):** NFV "Kalite modu" + "Maksimum kalite" checkbox'ları tarayıcıda doğrulandı
(`/run` formu `nesting_mode=nfv`+`nfv_quality=max`+CSRF; otonom JS bağlaması). Backlog #2 "görsel
doğrulama YAPILMADI" notu kapandı. (Login: `.env` ADMIN_PASSWORD var → test için `ADMIN_PASSWORD=""` ile başlat.)

**(A) occ-FFT paylaşımı — KAPSAMLI ARAŞTIRMA → NO-GO (6GB):** NFV her oryantasyonda occ'tan kırpılan
bölgeyi yeniden FFT'liyor; occ o parça için SABİT → occ-FFT tekrar = algoritmik redundancy (veri-hilesi
değil). 4 deney scripti (`scripts/c3_occfft_{profile,proto,batched}.py` + `c3_crop_swell.py`):
- **Profil:** occ-FFT decode'un ~%30'u, GENEL (plan1/2/3 sabit) — n=8 üst-sınır ~%26.
- **Naif ortak-crop:** BİREBİR (mm-eşlik her veride) AMA hız GENEL DEĞİL → **plan1 2.22× / plan3 0.96× /
  plan2 0.80×** = sadece plan1 kazanıyor (istisna), gerçek-ölçek 2 veri KAYBEDİYOR.
- **Maliyet-modeli:** N·logN gerçeği TERS tahmin (plan1'i "kayıp" der ama 2.22× kazandı) → güvenilmez.
- **Batched rfftn:** plan1 2.13× / plan3+plan2 OOM (n oryant aynı anda materyalize, 6GB yetmez).
- **KARAR:** (A) ailesi KESİN NO-GO (6GB). Birebir tasarım HEP doğruydu (kaliteyi bozmadı) ama güvenilir
  genel hız yok. **Datacenter (bol VRAM) → SÜPER BİLGİSAYAR backlog'una eklendi** (batched orada OOM etmez).
- **DERS:** kullanıcının "cross-dataset" + "önce test et" + "plan2'de denedin mi" disiplini 3 AYRI yanlış
  "üretime al"dan korudu. Detay memory backlog **7/7b/7c/7d/7e**.

**NET DURUM:** Kaliteyi-bozmayan KOLAY hız büyük ölçüde TÜKENDİ; mevcut NFV (xy-bbox+kademeli-z+
GPU-resident, 3-5.5×) zaten iyi optimize. Üretim kodu (src/) DEĞİŞMEDİ — tüm araştırma scripts/ deney.
`ANALIZ_NFV.md` = NFV toplam gelişim + overfit analizi (kalite %14-29, hız takası, overfit yönetimi).
